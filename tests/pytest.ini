[pytest]
addopts = --strict-markers -v
junit_family=xunit1
markers =
    auth: test authentication
    mediation: test Mediation app. you can run it locally when docker container is running
    debug: use to run one individual test for debugging
